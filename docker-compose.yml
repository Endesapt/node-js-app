version: "3.9"

networks:
  app-network:

services:

  app:
    image: node-js-app
    networks:
      - app-network
  
  nginx:
    image: nginx
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 80:80
    networks:
      - app-network